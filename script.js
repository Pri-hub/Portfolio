// Wait until the page is fully loaded
document.addEventListener("DOMContentLoaded", () => {
  // Log a message in the browser console
  console.log("Priya's portfolio site loaded successfully!");
});
